test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - py.test tests/ -v  --junitxml=$CIRCLE_TEST_REPORTS/junit/mongokat-$CIRCLE_NODE_INDEX.xml:
        parallel: true

machine:
  services:
    - mongodb